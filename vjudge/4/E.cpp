#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n, i;
    scanf("%d", &n);
    int a[n-1], b[n-1], x, y sa = 0, sb = 0;

    scanf("%d", &x);
    for(i = 0; i < n-1; i++)
    {
        scanf("%d", &a[i]);
        sa += a[i];
    }
    for(i = 0; i < n-1; i++)
    {
        scanf("%d", &b[i]);
        sb += b[i];
    }
    for(i = 0; i < n; i++)
    {
        if(sb >)
    }
}
