#include<bits/stdc++.h>
using namespace std;

#define FORAB(i, a, b) for(i = a; i < b; i++)

int main()
{
    int t, n, i, temp;
    scanf("%d", &t);
    while(t--)
    {
        scanf("%d", &n);

        int a[n];
        FORAB(i, 0, n)
            scanf("%d", &a[i]);




    }
}
