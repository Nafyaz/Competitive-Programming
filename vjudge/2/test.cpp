#include <stdio.h>
#include <string.h>

int main()
{
    int n, i, j, x=0, l;
    char a[5];
    scanf("%d\n", &n);
    for(i = 0; i < n; i++)
    {
        scanf("%s\n", &a);
        printf("%s\n",a);
    }
}
