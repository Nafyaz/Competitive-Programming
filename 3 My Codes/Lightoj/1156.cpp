#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t, i, caseno = 0, n, d, m;
    char s, c;
    cin >> t;
    while(t--)
    {
        cin >> n >> d;
        for(i = 0; i < n; i++)
        {
            cin >> c >> s >> c >> m;
        }
    }
}
