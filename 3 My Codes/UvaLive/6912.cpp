#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t, caseno = 0, i, j, n, k, p[1009];
    cin >> t;
    while(t--)
    {
        cin >> n >> k;
        for(i = 0; i < k; i++)
            cin >> p[i];
        sort(p, p + k);

        if
    }
}
