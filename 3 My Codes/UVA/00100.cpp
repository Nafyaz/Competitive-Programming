#include<bits/stdc++.h>
using namespace std;

int myfun(int n)
{

}

int main()
{
    int a, b, i, ilast, j;
    int clen[1000] = {0}; //clen is the cycle length

    while(scanf("%d %d", &a, &b))
    {
        printf("%d %d ", a, b);

        for(i = a; i <= b; i++)
        {
            j = 0;
            ilast = i;

            while(clen[i] != 1)
            {

            }
        }
    }
}
