#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n, m, k;
    string pref, q;

    while(cin >> n >> m)
    {
        if(!n && !m)
            break;

        while(m--)
        {
            cin >> pref;
        }

        cin >> k;
        while(k--)
            cin >> q;
    }
}
