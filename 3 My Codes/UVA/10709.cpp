#include<bits/stdc++.h>
using namespace std;

int main()
{
    int x[4], y[4];
    string s[2];

    while(1)
    {
        cin >> x[0] >> y[0] >> x[1] >> y[1] >> s[0];
        cin >> x[2] >> y[2] >> x[3] >> y[3] >> s[1];

        if(s[0] == "END" && s[1] == "END")
            break;

        if(s[0] == "L" && s[1] == "L")
        {
            cout << "0.00000\n";
            continue;
        }

        if
    }
}
