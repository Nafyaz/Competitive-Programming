#include<bits/stdc++.h>
using namespace std;

struct activity
{

};

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int a, t;

    cin >> a >> t;

    for(i = 0; i < a; i++)
    {
        cin >>
    }
}
