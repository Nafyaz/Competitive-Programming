#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n, m, k, a;
    while(1)
    {
        cin >> n >> m >> k >> a;
        if(!n && !m && !k && !a)
            break;
        k += a;

    }
}
