#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define ff first
#define ss second

int main()
{
    int n, k;
    pair<int, int> p[100009];

    cin >> n >> k;

    for(i = 0; i < n; i++)
        cin >> p[i].ff >> p[i].ss;

    sort(p, p+n);


}
