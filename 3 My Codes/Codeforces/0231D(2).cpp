#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int x, y, z, x1, y1, z1;
    int a[7], ans = 0;

    cin >> x >> y >> z;
    cin >> x1 >> y1 >> z1;
    cin >> a[1] >> a[2] >> a[3] >> a[4] >> a[5] >> a[6];

    x *= 2; y *= 2; z *= 2;
    x1 *= 2; y1 *= 2; z1 *= 2;

    if()
}
