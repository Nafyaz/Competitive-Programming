#include<bits/stdc++.h>
using namespace std;
#define ll long long

ll n, m;
ll w[100005];

int main()
{
    ll i, q, l, r;

    cin >> n >> m;

    for(i = 1; i <= n; i++)
        cin >> w[i];

    cin >> q;

    while(q--)
    {
        cin >> l >> r;


    }
}
