#include<bits/stdc++.h>
using namespace std;

int a[200005];

int main()
{
    int t, n, k, i;

    cin >> t;

    while(t--)
    {
        cin >> n >> k;

        for(i = 0; i < n; i++)
        {
            cin >> a[i];
        }
    }
}
