#include<bits/stdc++.h>
using namespace std;
#define ll long long

ll a[502];

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    ll t, n, i;

    cin >> t;

    while(t--)
    {
        cin >> n;

        for(i = 0; i < n; i++)
        {
            cin >> a[i];
        }
    }
}
