#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
    ll T, n, m, l, r, x;

    cin >> T;

    while(T--)
    {
        cin >> n >> m;

        for(i = 0; i < m; i++)
        {
            cin >> l >> r >> x;


        }
    }
}
