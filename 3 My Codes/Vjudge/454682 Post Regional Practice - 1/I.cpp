#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
    ll t, n, a[1003], i, j;

    cin >> t;

    while(t--)
    {
        cin >> n;

        for(i = 0; i < n; i++)
            cin >> a[i];

        for(i = 0; i < n; i++)
        {
            for(j = 0; j < n; j++)
            {

            }
        }
    }
}
