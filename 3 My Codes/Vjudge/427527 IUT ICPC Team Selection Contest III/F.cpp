#include<bits/stdc++.h>
using namespace std;
#define ff first
#define ss second

string s;

bool is_equal(int st, string code)
{

}

int main()
{
    int t, st, ans;
    char letter;
    string code
    cin >> t;

    while(t--)
    {
        cin >> n >> m;

        map<string, int> freq;
        ans = 1;

        while(m--)
        {
            cin >> letter >> code;
            freq[code]++;
        }

        cin >> s;

        st = 0;
        for(auto u : freq)
        {
            if(isequal(st, u.ff))
            {

            }
        }
    }
}
