#include<bits/stdc++.h>
using namespace std;

long long func(long long i)
{
    if(i == n)
        return 0;
    long long nowsum, sum = 0;
    for(long long j = i+1; )
}

int main()
{
    long long n, i, j, sum = 0;
    scanf("%lld", &n);

    long long a[n + 2], b[n + 2];

    a[0] = a[1] = b[0] = b[1];

    for(i = 2; i < n+2; i++)
        scanf("%lld", &a[i]);
    for(i = 2; i < n+2; i++)
        scanf("%lld", &b[i]);

    j = 0;
    for(i = 0; i < n+2; i++)
    {

    }
}
