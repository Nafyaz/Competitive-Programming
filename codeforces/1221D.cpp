#include<bits/stdc++.h>
using namespace std;
int main()
{
    int T, i, h[300010], b[300010], n, cost;
    cin >> T;
    while(T--)
    {
        cost = 0;
        cin >> n;
        for(i = 0; i < n; i++)
            cin >> h[i] >> b[i];
        for(i = 1; i < n; i++)
        {
            if(h[i] == h[i-1])
            {
                if(b[i])
            }
        }
    }
}
